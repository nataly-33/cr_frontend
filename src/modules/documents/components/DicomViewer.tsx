/**
 * Re-exporta el visor DICOM del módulo dicom.
 * Este archivo mantiene compatibilidad con imports existentes.
 *
 * Para uso completo del visor DICOM, usar:
 * import { DicomViewer } from "@modules/dicom";
 */
export { DicomViewer } from "@modules/dicom/components/DicomViewer";
